server.name: quantum-kibana
server.port: 5601
server.host: "0.0.0.0"

# Connect Kibana dashboard to the Elasticsearch host endpoint
elasticsearch.hosts: [ "http://elasticsearch:9200" ]

# Set index query time thresholds
elasticsearch.pingTimeout: 1500
elasticsearch.requestTimeout: 30000

i18n.locale: "en"
